# 前端、设置与数据管理

> 本专题是 [DOCS.md](../DOCS.md) 索引的权威文档之一。修改前端布局与交互、`.env` 设置、
> 重启/网页更新入口或历史数据清理时必须完整阅读并同步更新。

## 4.10 前端（白色浅色主题）

前端按页面职责拆分：`App.tsx` 负责认证、全局运行状态和七个标签页编排，设置及更新/备份/日志/
重启/Custom API 组件位于 `SettingsPanel.tsx`，统一 REST 错误与未授权处理位于 `api.ts`。拆分保持
既有组件导出兼容，避免测试或其他调用方依赖内部文件位置。

顶部标签栏将功能分为七页，避免单页过长；WebSocket 状态/决策推送与账户轮询在所有标签页
间共享，不随切换中断。决策记录变化后通过 WebSocket 自动更新最近 10 次运行的完整决策；连接断开后每 2 秒
自动重连，并以每 15 秒 REST 轮询作为漏消息兜底，无需手动刷新页面。

总览、运行用量、硬风控、候选池、决策置信度、账户、24 小时 Provider 运维指标
与回测摘要均提供悬浮指标定义；带点状下划线的名称或数值会在光标停留约 80 ms 后显示统计窗口、分母、
计算口径或关键限制。指标说明使用前端自有提示层，悬浮时光标保持普通箭头，不再等待浏览器原生提示。
Token 与成本定义同时注明 Provider 计量差异和订阅账单边界，避免把折算值
理解为实际扣费。

提示层用 `position:fixed` + CSS 锚点定位（`anchor-name`/`position-anchor`/`position-area`）挂在
锚点上，并以 `position-try-fallbacks` 在提示会越过视口右缘时翻转到锚点右对齐。用 `fixed` 而非
`absolute` 是必需的：`visibility:hidden` 只隐藏、不移除布局，绝对定位的隐藏提示会把自身宽度计入
文档可滚动溢出，使总览页在 1350 px 窗口下无悬浮也横向滚动 200 px；`fixed` 元素不参与祖先的可滚动
溢出，从结构上消除该问题。因此前端要求浏览器支持 CSS 锚点定位（Chrome 131+ / Safari 26+）；
Firefox 尚未实现，提示层会回落到静态位置且不跟随滚动，功能与数据不受影响。

| 标签页 | 面板 | 内容 |
|---|---|---|
| 总览 | 引擎控制（hero）| 系统状态、分析周期、正式决策输入（标准决策 / AI 分析辅助 SHADOW）、每周期候选标的数、正式决策硬超时、一次真实批量试跑的候选/额外持仓/实际分析数量、容量与调用详情、当前周期/标的/阶段，以及试跑、试跑并启动、运行一次、启动、停止、紧急熔断按钮；辅助模式明确显示固定 1 倍、T1 正式止盈字段、T2 影子评估且不提交订单，本地规则不可选择该模式；“试跑并启动”只在同一次真实试跑成功后自动进入持续运行，失败则保持停机。结构化分析失败时错误横幅只显示有限校验摘要，不展示模型输入片段或 Pydantic 内部文档 URL。宽屏为状态与控制两栏，六个控制按钮并排；下方实时显示本次或上次运行的 Token、等效成本、调用数与时长 |
| 总览 | 01 模型接入 | 宽屏左栏，整块默认展开并可独立折叠，折叠后保留编号、标题和“单一 Provider”摘要；切换到其他标签页再返回时保留本次页面会话中的折叠状态。每张 Provider 卡片内集成接入状态并互斥选择，一次运行只能使用一个 Provider，选中态即为唯一运行来源，不再在卡片列表下方重复显示“本次运行 Provider”或“实际承载”；外部模型可配置模型与推理强度，Codex 额外显示并可选择 ChatGPT App / Codex CLI 接入来源且显示当前登录邮箱；选择并应用独立 Codex CLI 后，可直接启动设备码登录、打开授权页、查看一次性代码、取消等待，并经二次确认登出，无需进入 VPS 终端。登录有效时自动查询并可手动刷新 Codex 剩余额度，按 CLI 实际返回的套餐、窗口时长和重置时间逐项显示；Team 仅返回月窗口时不会出现周额度。标准本地规则显示固定版本与零 Token/成本，只作为确定性基准；结构与衍生品上下文由外部 LLM 解释，不再显示固定阈值本地实验卡。浏览器宽度不超过 1200px 时，总览双栏提前折叠为单栏，避免缩放后的 Provider 配置控件越界覆盖右栏 |
| 总览 | 02 硬风控边界 | 宽屏右栏顶部，整块默认展开并可独立折叠，折叠后保留编号、标题和只读摘要；切换标签页后保留本次页面会话中的折叠状态；集中展示当前候选标的数、10 倍最大杠杆、当前运行实例的滚动 24 小时亏损熔断比例、1% 单笔风险、4% 组合止损风险、原始盈亏比必须大于 1.15:1、保证金与持仓模式等边界 |
| 总览 | 03 动态候选池 | 宽屏右栏紧接硬风控，填充模型列表右侧空间；整块默认展开并可独立折叠，折叠后保留编号、标题和测试网过滤摘要，切换标签页后保留本次页面会话中的折叠状态；展开后展示全市场扫描结果并可手动刷新，默认只显示评分前 5 个，可继续展开查看全部 |
| AI 分析 | 10 独立 AI 行情分析 | 可输入一个 USDT 永续标的，或点击“分析候选”把总览正式引擎当前候选池前 N 名按排名排队研究；界面明确列出这批候选、显示完成数并可整体取消，不追加已有持仓。每个标的使用当前唯一外部 Provider 独立冻结、调用和保存。候选条下方可显式启停每 15 分钟自动研究，展示下一 UTC 边界、本轮状态及上一轮候选/发起/跳过计数与标的；页面关闭不停止后端轮询，服务重启则安全回到关闭。自动轮每个标的独立发送，方向计划尚处于等待入场、active、T1 部分止盈或结果刷新失败时跳过；同 K 线顺序不确定视为终态，不再阻塞下一轮分析。停止只取消未来轮次，当前轮可从分析历史选择活动记录后用既有取消入口终止。候选与历史之间的“历史盈亏表现”允许调整每笔固定名义金额和每笔固定止损金额，分别显示累计 USDT 盈亏、胜率、平均回报或 R；样本只取已入场且明确结算的计划，并明示未计手续费、滑点和资金费。左栏分析历史提供“方向 / 偏多 / 偏空 / 全部”筛选；默认“方向”显示偏多、偏空和尚无结果的任务并过滤观望，避免批量处理中任务消失，切换筛选时会清空不符合条件的当前详情。每条历史在方向下方直接显示“尚未评估”或最新计划结果；正式辅助周期生成的记录另标“正式 SHADOW”，详情把 T1 标为固定止盈、T2 标为 SHADOW。栏头“批量更新结果”会更新当前加载的最多 30 条已完成记录，单条失败不阻断其余项并显示成功/失败计数，同时刷新历史表现。右栏显示方向/观望、锚点、5m/15m/1h 冻结 K 线、entry/结构 stop/T1/T2 或观望区间、双目标盈亏比、情景概率与触发/路径/失效、管理计划、证据、缺失数据影响和 Token/版本审计；新分析的摘要、理由、情景、计划、证据及程序生成的结果说明显示简体中文，JSON 键、枚举和标准市场缩写保持英文，旧记录保持原文。观望区间使用专用布局类并与情景卡片保持正常纵向间距，不复用候选数滑杆样式。“更新结果”按需显示等待入场、未入场止损、未入场 T1、active、T1 部分止盈、T2、结构止损、T1 后保本或同 K 线顺序不确定；“未入场”结果明确表示计划没有成交。5m 歧义会自动尝试用完整 1m K 线细分，成功时结果说明披露该依据，缺线或同一分钟仍冲突时继续显示不确定。正式引擎运行或没有外部 Provider 时禁用发起；该页不调用下单接口。窄屏改为单列，图表自身横向滚动且不得撑宽整页。 |
| 总览 | 04 决策与风控 | 只展示归属于正式运行的决策，按持久化运行分组并显示运行编号、起止时间、状态、该次实际执行的 7 位 Git 版本、配置摘要、决策数、停止原因、价格已实现毛利、当前未实现盈亏、可归属手续费、资金费完整性、交易净盈亏、已完成平仓胜率及当前未平仓标的数；旧运行没有版本字段时明确显示“版本未记录”，不得用当前版本回填。无法可靠归属到运行的资金费明确显示未知且不混入交易净盈亏，停止后手动平仓仍更新归属结果，运行指标每 5 秒刷新。每个运行组可独立折叠，运行中默认展开，已结束运行默认收起，展开后显示该次运行的全部决策；每条决策明确显示同批标的共享、不可逐条相加的批次耗时，后续风控或执行额外增加至少 0.10 秒时另显示整笔耗时。组内将 LLM 意图、硬风控与执行尝试合并为一条审计事件；AI 分析辅助中通过风控但未执行的记录显示“影子放行 · 未提交订单”。可按下单成功、下单失败、否决、HOLD、仅推理筛选，并展开失败阶段、交易所错误、紧急回补、损失估算和止盈后 15/30/60 分钟重入 shadow 命中窗口；被否决且具有完整保护价的开仓/加仓还提供“评估后续表现”，展示假设放行后的等待入场、观察中、止盈、止损、TTL 未成交、入场前失效及顺序不确定状态，并明确它不是实际成交。不提供几乎不会持久存在的 `approved` 单独筛选按钮 |
| 账户 | 06 账户与订单 | 集中展示币安测试网配置/用户流/启动对账状态、钱包与权益、交易所实时持仓；持仓按标记价显示 USDT 名义价值与初始保证金，并按开仓均价和多空方向显示止盈/止损百分比及原始盈亏比（不含手续费和滑点）；表格将方向/杠杆、持仓价值/保证金、均价/标记价分别合列，并将止损、止盈分列紧凑展示；同时展示含开仓、止损、止盈、回补和手动平仓的统一成交生命周期、五组移动止损 shadow 参数，以及 `1R / 25% + BE`、`1R / 50% + BE` 两组部分止盈 shadow 实验；两类影子事件均展示候选/目标与首次观察成交价，部分止盈另显示交易所步长对齐数量和未扣费价格毛利；每 5 秒刷新；引擎停止时可二次确认后按标的市价平仓 |
| 回测 | 05 回测运行 | 重放表单、回测列表、详情（权益曲线、交易明细、分组统计）|
| 运维 | 07 模型运维 | 只展示 Provider 延迟/调用量/错误率/Token/等效成本；测试网账户与持仓统一归到账户页 |
| 数据 | 08 数据管理 | 按类别删除历史数据（见 4.12）|
| 设置 | 09 设置 | 软件更新、服务器备份、CandlePilot 专用日志清理、重启、Custom API 端点与分区 `.env` 表单（见 4.11）|

决策列表对非 `HOLD` 动作显示「执行置信度」；对 `HOLD` 将同一字段弱化显示为「机会强度」，
表示没有可执行动作时残留的方向性机会，避免误解成模型对 HOLD 结论本身缺乏信心。
非 `HOLD` 决策将风控结论与执行结论分层显示：`executed` 表示下单及所需保护流程完成，
`execution_failed` 表示风控已放行但入场、保护或回补阶段失败；能够确认不利价差时，列表直接显示
失败损失估算，详情同时展示入场与回补数量/均价。决策细目把硬风控定量结果标为「最终下单数量」；
仅在入场下单成功且实际成交数量、均价均可确认时，执行结果额外显示实际「成交额」与「保证金」，
前者按成交数量乘成交均价计算，后者按成交额除以本次杠杆估算且不包含手续费。决策 API 优先使用
由 Binance 用户流持续更新的执行记录补全 REST 立即响应中尚未返回的成交数量与均价，因此延迟确认
的成功成交也会显示这两个值。`approved` 还用于本地限价意图等待触发的持久
状态；前端将后者单独显示为「等待触发」，并展示精确截止时间，避免误解为已经向交易所下单。
每条决策在 Provider 旁直接显示该次调用实际使用的模型名和推理强度；这两个值随推理结果写入
审计，不读取当前配置回填；显式强度直接显示为 `low` / `medium` / `high` 等原值，不重复添加
「推理」前缀，显式留空并使用 Provider 默认值的记录显示「默认推理强度」。
决策详情的入场、止损、止盈、结构锚点、触发、失效位和成交价格使用同一自适应精度：最少 4 位、
最多 8 位小数。这样高价标的保持紧凑，低价标的不会被固定四位显示截断；AI 原始盈亏比与下单前
盈亏比都显示四位小数，便于直接核对接近 `1.15:1` 的边界。

## 4.11 设置（编辑本地 .env）

- 前端「设置」标签页按分区列出通用 `.env` 配置项（运行模式与服务、决策与运行、运行 Provider、
  币安测试网），`GET /api/settings` 提供这些字段的元数据与当前值，`POST /api/settings` 保存；
  Custom API 端点由同页独立的 `GET/POST /api/custom-providers` 表单管理。
- **密钥默认遮罩、Custom API Key 可按需查看**：设置列表中的币安测试网 Key/Secret，以及内嵌
  `api_key` 的 Custom API 端点默认只返回是否已配置与掩码尾号（如 `sup…abcd`）。
  Custom API 端点允许用户主动点击“显示密钥”，通过
  `GET /api/custom-providers/{id}/api-key` 只读取该端点已保存的完整 Key；响应强制
  `Cache-Control: no-store` 与 `Pragma: no-cache`，再次点击可从前端状态中隐藏。该能力只存在于
  本项目强制绑定的后端以及通过 HTTPS/鉴权进入的远程前端；不会自动加载所有 Key，也不开放
  自定义请求头或币安 Secret。
  前端保存时仍只发送**被改动过的键**，因此仅查看不会把密钥重新写回或用掩码覆盖原值。
  通用设置接口也把密钥字段的空字符串解释为“保持原值”；即使用户输入后又清空，已有币安凭据
  也不会被误删。普通非密钥字段仍可用空值恢复默认配置。
- **只写文件、不改运行中的进程**：保存只写 `.env`，**重启后生效**；界面明确提示。已在 shell
  中 `export` 的同名变量在运行时优先级仍然更高。
- **保存前整体校验**：用启动时同一套解析器校验候选配置（`Settings.from_mapping`，纯函数，
  不篡改 `os.environ`），并补充引擎/调度器构造期才会做的范围检查（周期取值、每周期候选标的数、
  仅 localhost 绑定）。**任何不合法的值都会在文件被修改前拒绝**，不会写出一个下次启动直接
  崩溃的 `.env`。
- **Provider 必须唯一且引用完整**：为兼容现有部署保留 `CANDLEPILOT_PROVIDER_CHAIN` 名称，但值必须
  恰好一个，不能再用逗号配置主备；该名称必须对应本地规则、Codex、Claude Code 或同一候选配置中
  实际存在的 `openai-compatible:<id>`。通用设置表单与 Custom API 表单均校验**修改后的完整
  候选配置**；修改或删除当前选中的 Custom API ID 会返回 422，文件保持不变并明确
  列出失效字段与 Provider。需要改 ID 时，先改选其他 Provider，再修改 ID，最后按需选回新 ID。
  CLI 启动和 `doctor` 使用同一检查；若用户手工把
  `.env` 改成引用失效状态，进程会明确失败，不会忽略条目、改选其他 Provider 或让界面显示一套
  而实际运行另一套配置。设置页在单一 Provider 输入框下直接列出面向 `.env` 的完整填写方式：本地基准
  使用 `local`，Codex 使用 `codex`、Claude Code 使用 `claude-code`，Custom API 使用
  `custom:<id>`；一次只填其中一项。
- **Custom API 端点用表单编辑，不写 JSON**：`GET/POST /api/custom-providers` 提供逐字段的增删改
  （ID、Base URL、API Key、模型、协议、推理强度、是否需要 Key），前端负责序列化成
  `CANDLEPILOT_CUSTOM_LLM_PROVIDERS_JSON`。列表 GET 只返回 `api_key_configured` 与掩码，显式
  reveal 端点按上条规则返回单个 Key；POST 省略 `api_key` 表示保持原值、`""` 表示清除、给值表示替换。
  `extra_headers` 的值同样是密钥，GET 只返回**头名称**，保存时未提交则原样保留。保存前用启动
  解析器校验，并额外校验 Base URL（Provider 构造只把非法 URL 记为配置错误而不抛错，
  不校验就会存下一个"看起来保存成功、实际不可用"的端点）。
- **模型配置可直接验证**：「模型接入」卡片修改模型或推理强度后，测试按钮显示「应用并测试」，
  先把当前草稿应用到运行中的 Provider，再立即用合成快照发起真实调用；无需先单独点击「应用」。
  Provider 即使因缺少 Key、认证失败或其他配置问题显示 `MISSING` / `LOGIN`，测试按钮仍可用，
  后端会返回具体失败原因。只有引擎运行、其他模型测试/回测占用，或已有操作正在执行时才禁用。
- **首页可切换 Custom API 计费厂商**：Custom API 的「模型接入」卡片同时显示 models.dev
  计费厂商，可与模型、推理强度一起「应用」或「应用并测试」。切换后立即更新该运行进程的模型
  候选和等效成本折算；留空表示成本未知，运行预算不会按该端点触发。首页修改是运行时配置，
  重启后恢复 `.env`；需要持久化时仍在「设置」页保存。
- **写入安全**：保留注释与键顺序，未知键不动，新键追加；原子替换（临时文件 + `os.replace`），
  文件权限 `0600`；值按原样写入不转义——读取端（`load_dotenv`）只剥离首尾引号且不做反转义，
  转义会让 JSON 配置在下次启动时损坏。保存前以与启动相同的解析器校验完整候选配置；数据库字段
  只接受带普通持久文件路径的 `sqlite+aiosqlite` URL，不能保存一个要到重启时才暴露的同步驱动、
  其他方言、`:memory:`、`file:` URI 或损坏连接串。
- **重启后端**（`POST /api/restart`，前端设置页按钮）：用当前 `.env` 重新执行后端进程，让保存
  的设置生效。**引擎、调度器、Provider 探测或回测仍在运行时都会被拒绝（409）**，
  避免打断交易或留下不完整任务；接受重启后立即拒绝新的写请求，并按调度器、引擎、行情、
  Broker、用户流、数据库的顺序关闭资源，再 `os.execve` 重新执行自身。接口先响应（进程被替换后无法
  再发响应），前端轮询 `/api/health/live` 直到恢复再刷新。
  **重启前会剔除本进程当初由 `.env` 注入的环境变量**（`DOTENV_INJECTED_KEYS`）：`load_dotenv`
  不覆盖已有变量，若继承旧值则重写后的 `.env` 会被忽略、重启等于白做；shell 中显式 `export`
  的变量会保留（它本就优先于 `.env`）。重新执行统一走 `.venv/bin/python -m candlepilot.cli`，
  因此 `candlepilot serve` 与 `.venv/bin/python -m candlepilot.cli serve` 两种启动方式都能恢复。
- **网页检查与安装更新**（`GET /api/update/status`、`POST /api/update/check`、`POST /api/update`，
  前端设置页「软件更新」）：仅在 Linux VPS 安装器已经部署 root 更新助手时启用。「检查更新」
  只从无内嵌凭据的 GitHub HTTPS `origin` 获取当前分支，比较本地与远端提交并确认是快进关系，
  不改工作树或启动 root 更新器；发现新版本后才启用独立的「安装更新」，并继续要求用户确认。确认后，后端通过固定的无参数
  `/usr/local/sbin/candlepilot-web-update` 在 `/run/candlepilot-update` 创建固定格式的动作与请求文件；root 管理的
  `candlepilot-update.path` 只监听这个固定路径并启动独立 `candlepilot-update.service`。后端不调用
  `sudo`，主服务可继续启用 `NoNewPrivileges=true`；更新任务也不在应用服务的 cgroup 内，因此停止
  `candlepilot.service` 不会杀掉更新器。引擎、调度器、
  Provider 探测或回测活动时返回 409；接受更新后拒绝新的写请求，前端允许后端暂时
  离线并轮询持久化状态，成功后自动刷新，失败时显示安装器的安全摘要。状态接口只公开阶段、时间、
  新旧提交号、备份路径和有限长度消息，不公开 root 日志。
  launcher 由 root 持有，只接受无参数更新以及预定义的备份刷新/删除动作；删除参数必须是严格备份 ID，
  应用用户只能写入由 systemd tmpfiles 创建的固定请求目录，无权选择文件路径或直接启动 root 命令；
  worker 和安装器副本同样由 root 持有，绝不以 root 运行应用用户
  可写目录里的脚本。更新仍只接受配置的 GitHub origin 与分支快进，并沿用下文的备份、健康检查
  和失败回滚。
- **服务器备份**（`GET /api/backups`、`POST /api/backups/refresh`、
  `POST /api/backups/{id}/delete`，前端设置页「服务器备份」）：展示 root worker 生成的脱敏清单，
  只含标准备份 ID、UTC 创建时间、来源提交、占用字节数和保护标记，不返回备份路径或内容。清单按
  新到旧排列，最新一份只显示「保留」；旧备份必须二次确认后删除。前端不能绕过最新/仅存备份保护，
  删除与软件更新互斥，完成后显示实际释放空间并刷新清单。初次安装或旧助手尚未生成清单时可手动刷新。
- **CandlePilot 日志**（`GET /api/logs`、`POST /api/logs/clear`）：设置页显示最近维护结果，清理前
  必须二次确认。正式引擎、调度器、Provider 探测或回测活动时拒绝；动作与更新、备份、重启互斥，
  由同一受限 root worker 固定执行 `candlepilot` journal 命名空间的轮转和 vacuum，前端不能传
  路径、保留期或命令。首次启用隔离会短暂重启服务，前端容忍断线并等待状态文件中的最终结果；
  删除不可恢复，但不会触及 SSH、Nginx 或其他服务日志。
- `.env` 路径默认为工作目录下的 `.env`，可用 `CANDLEPILOT_ENV_FILE` 指定。

## 4.12 数据管理

- 前端「数据管理」面板与 `POST /api/history/clear` 可按类别删除历史数据：
  模型调用与决策（含对应正式运行的自动回放数据）、风控决策、订单成交、测试网事件、告警历史、移动止损记录、部分止盈实验、**回测记录**、**旧盘口采集数据**、
  独立 AI 行情分析（含冻结输入、Prompt 和原始输出）、行情缓存、定价缓存。回测记录含运行、逐模型结果与每条决策（后两者由外键级联删除，
  `PRAGMA foreign_keys=ON` 已开启）。
- 风控记录可独立保留；只删除“模型调用与决策”时，与其关联的风控记录不会被误删，但
  `inference_id` 会由外键 `ON DELETE SET NULL` 清空，避免悬空编号日后指向一个新复用的推理主键。
- 可用「全选 / 取消全选」一次切换全部类别；删除仍需**两步确认**，写入结构化日志。
- 正式决策、回测或试跑任一任务活动时，后端拒绝全部历史清理请求并返回 409；前端在
  正式决策运行中同步禁用删除入口。必须先让活动任务停止，避免删除当前运行的审计链、把成本
  看护累计值归零，或让后台任务继续写入一个已经被删除的回测上下文。
- `book_captures` 只为兼容既有数据库保留，不再写入，也不再作为新回测输入；设置页仍可按
  “旧盘口采集数据”清理遗留行。完整盘口回测统一使用正式引擎自动保存的数据集。
- **绝不触及** `runtime_state`（紧急锁定）与 `schema_migrations`，
  故清历史不会解除熔断锁或破坏迁移状态。
