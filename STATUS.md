# CandlePilot 开发状态

最后更新：2026-07-14

本文件是项目实现状态的唯一进度台账。每完成一个功能，必须在同一个 Git 提交中更新本文件。

## 当前结论

CandlePilot 已具备本地 MVP 的主要骨架：订阅认证 LLM、币安公共行情、动态选币、
确定性风控、模拟成交、测试网基础下单、审计、历史回测和中文控制台。

当前版本仍不适合真钱实盘，也未达到长期无人值守运行标准。近期主线是实时行情、
完整模拟撮合、测试网对账与恢复。

## 已完成

- [x] 保存实施方案 `PLAN.md`。
- [x] Python/FastAPI/SQLite 后端基础和 React/TypeScript 控制台。
- [x] Codex Auth：自动检测 Codex App 内置二进制，再回退到独立 `codex` CLI。
- [x] Claude Code Auth：检测独立 `claude` CLI 并复用登录状态。
- [x] LLM 临时目录、环境变量过滤、超时、单并发和严格 `TradeIntent` Schema。
- [x] 可选模型与推理强度：Codex 传 `-m`/`-c model_reasoning_effort`，Claude 传
  `--model`/`--effort`；默认取自环境变量，也可在控制台运行前经 `/api/providers/config` 修改。
  控制台模型为下拉选择（选项来自 models.dev 目录，按 Provider 过滤并含 CLI 别名），并保留
  「自定义」输入以支持目录外模型。
- [x] 币安 USDT 永续合约发现、交易规则、K 线、资金费率、盘口和 24h ticker REST 数据。
- [x] K 线和资金费率历史分页下载。
- [x] 资金费率按实际结算 K 线映射。
- [x] 历史行情 Parquet 本地缓存。
- [x] 动态候选池：上市时间、完整度、价差、成交额、波动率和趋势过滤排名。
- [x] EMA、RSI、ATR、收益率和成交量特征。
- [x] 1m、5m、15m 对齐调度，每周期最多分析 5 个候选。
- [x] 止损、杠杆、单笔风险、仓位数、保证金、日亏熔断和交易所精度硬风控。
- [x] 基础模拟成交、持仓、手续费、滑点和紧急平仓。
- [x] 币安当前官方 Demo 测试网签名请求、逐仓、杠杆、下单、保护止损、超时查询和紧急平仓。
- [x] SQLite 保存模型推理、风控决定、执行结果和回测记录。
- [x] 单标的事件驱动回测：下一根 K 线成交、手续费、滑点、资金费率、止损止盈。
- [x] 已审计 LLM 决策的历史重放，避免回测时重复调用模型。
- [x] 控制台 Provider 选择、启停、候选池、信号、回测表单和回测列表。
- [x] 白色浅色控制台主题。
- [x] `candlepilot doctor` 本地诊断命令和 localhost 监听限制。

## 部分完成

- [x] 实时事件：公共组合流自动管理、去重、乱序丢弃，并在重连后执行 REST 回补。
- [x] 候选池：启动后立即扫描，并每分钟自动刷新和轮换。
- [x] 模拟账户：现金、仓位、挂单和成交状态持久化，重启后恢复并继续实时保护。
- [x] 测试网：支持安全下单、启动对账、状态未知订单恢复、可审计用户流及部分成交保护。
- [x] 熔断：锁定状态持久化，重启后保持，并在下一 UTC 日自动解锁。
- [x] 回测：支持单标的与等权资金多标的组合，方向/退出原因/市场状态分组统计和详情可视化。
- [x] 控制台：显示实时行情连接、订阅和事件状态，以及模拟账户权益、持仓、订单成交和风控决策；缺少模型延迟与调用量运维指标。
- [x] Provider：支持统一取消、能力声明和显式单次主备故障切换。

## 未完成（按优先级）

### P0：可靠模拟与测试网运行

- [x] 币安公共 K 线、mark price、盘口和 ticker WebSocket 客户端。
- [x] 币安账户与订单用户数据流：listenKey 续期、自动重连、排序、测试网隔离和 SQLite 事件审计。
- [x] WebSocket 自动重连、心跳、去重、乱序处理和 REST 回补。
- [x] 模拟模式的行情驱动限价单、止损、止盈和持续盯市核心。
- [x] 测试网启动时账户、持仓和订单对账，无保护仓位会阻止启动。
- [x] 部分成交后撤销入场余量；保护止损使用 `closePosition` 自动覆盖当前全部仓位。
- [x] 状态未知订单不重复下单；429/418 退避重试；时间戳错误自动重新同步。
- [x] 紧急锁定持久化和下一 UTC 日自动解锁。
- [~] 测试网连续运行 24 小时验收：已提供 `candlepilot acceptance` 审计工具，按发布不变量
  （连续运行时长 ≥24h、client order id 唯一、测试网持仓已对账且带保护、每笔成交可追溯到
  模型输出与风控判定）判定，证据不足时判为不通过而绝不虚假放行。**尚未记录真实的 24 小时
  测试网运行**，因此本项仍未验收完成。

### P1：策略和回测完整性

- [x] 每分钟自动刷新候选池。
- [x] 1m/5m/15m 共享趋势、动量、波动和量能特征，以及基差、持仓量、20 档盘口与近期成交失衡。
- [x] 同标的跨周期串行评估；禁止相反方向并发开仓，同向增仓必须显式使用 `ADD`。
- [x] 显式独立 LLM 历史回测：滚动无前视快照、Provider 认证、调用预算和推理审计。
- [x] 多标的组合回测：等权资金子账户、时间对齐组合权益、组合回撤与分标的结果。
- [x] 已有 Sharpe、Sortino、平均盈亏比、换手、时间敞口，以及方向、退出原因和市场状态（趋势/震荡/高波动，无前视趋尾窗口分类）分组统计。
- [x] 回测摘要列表、按 ID 详情、交易明细和控制台权益曲线图。
- [x] SHA-256 数据版本、显式 Prompt 版本、模型标识和 CLI Provider 版本记录。

### P2：控制台和运维

- [x] 持仓、订单、成交、权益和风险事件：后端查询接口（`/api/account/portfolio`、
  `/api/account/positions`、`/api/orders`、`/api/fills`、`/api/risk-events`）与控制台
  「账户与风险」面板（权益概览、持仓、订单成交、风控决策），每 5 秒自动刷新。
- [x] 模型延迟、调用量和错误率提供 1-720 小时时间窗口聚合 API
  （`/api/metrics/providers`，含平均/P95 延迟、错误数、模型分布、Token 用量与等效
  API 成本估算）；Codex 用 `--json` 事件流解析模型输出与 input/cached/output token 分项、
  模型名取自 `~/.codex/config.toml`，成本经 models.dev 逐 token 折算管线（
  `https://models.dev/api.json`，本地缓存 24h、离线回退，缓存读为输入子集且支持长上下文分层）
  计算；Claude 直接用 CLI 自带 `total_cost_usd`。订阅计划不按次计费，成本仅为折算估算，
  无法定价的模型显示为空；测试网账户状态 API
  （`/api/testnet/account-status`）提供余额摘要、非零持仓、启动对账和用户流状态且不暴露凭据；
  白色控制台「模型与测试网」面板每 5 秒刷新上述运维数据。
- [x] SQLite/SQLAlchemy 顺序数据库迁移、版本记录和幂等升级机制。
- [x] 存活与就绪健康检查已提供（`/api/health/live`、`/api/health/ready`），就绪检查覆盖
  数据库迁移版本与测试网 Broker 配置；HTTP 请求使用 JSON 结构化日志与 request ID，运行指标
  API（`/api/metrics/runtime`）提供请求量、错误率、并发数、平均/P95 延迟和状态码分布；告警 API
  （`/api/alerts`）覆盖紧急锁定、测试网配置/保护/用户流、API 错误率及模型错误率/P95 延迟。
  本地告警通知渠道已完成：告警首次触发与解除时去重并写入 JSON 结构化日志（供外部日志采集
  转发）及 `alert_events` 审计表，可经 `/api/alerts/history` 查询。对外发送到第三方服务
  （Webhook/邮件/IM）的渠道刻意不实现，以免在本地优先系统中产生未经授权的外发动作。
- [~] 依赖锁定、Docker、CI 和发布流程：Python 依赖已锁定（`requirements.lock`，从工作
  `.venv` 冻结）；前端已有 `pnpm-lock.yaml`；GitHub Actions CI（`.github/workflows/ci.yml`）
  在 push/PR 上运行 Ruff、Pytest 与前端 `tsc + vite build`。Docker 和发布流程待完成。

### 暂不实施

- [ ] 币安真钱实盘入口。
- [ ] 美股行情和券商适配。
- [ ] 新闻、社交媒体和链上数据。

## 当前验证状态

- 后端测试：142 项通过。
- Python 静态检查：Ruff 通过。
- 前端：TypeScript 与 Vite 构建通过。
- 币安公共 WebSocket：BTCUSDT `bookTicker` 真实连接冒烟测试通过。
- 控制台：实时行情状态在本地服务中完成浏览器渲染验证。
- Git：`main` 工作区干净；本轮功能提交已推送远端。

## 更新规则

1. 每个可独立验收的功能使用一个 Git 提交。
2. 功能代码、测试、文档和本文件必须放在同一个提交中。
3. 只有测试通过并完成必要的实际检查，才能将项目标记为 `[x]`。
4. 部分实现使用 `[~]`，尚未开始或不可用使用 `[ ]`。
5. 不以盈利作为完成标准，以无前视偏差、可审计、风险边界和故障恢复为验收标准。
